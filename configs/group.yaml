processed_dir: data/processed
model_run_name: lightfm_baseline   # which models/<name>/model.pkl to load
user_ids: [37603, 26676, 164243]   # a simulated divergent group (from eval groups.json)
context:
  setting: party      # casual | party | competitive
  familiarity: friends  # friends | strangers
constraints:
  n_players: 4
  max_playtime_min: 60
aggregation:
  method: fairness_penalty  # average | least_misery | fairness_penalty
  fairness_weight: 0.5
top_k: 10
run_name: group_example
